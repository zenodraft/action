"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.deposition_create_concept = void 0;
const node_fetch_1 = require("node-fetch");
const get_api_1 = require("../../helpers/get-api");
const deposition_create_concept = (token, sandbox, verbose = false) => __awaiter(void 0, void 0, void 0, function* () {
    if (verbose) {
        console.log(`creating a new, empty version in a new concept...`);
    }
    const api = get_api_1.helpers_get_api(sandbox);
    const endpoint = '/deposit/depositions';
    const url = `${api}${endpoint}`;
    const headers = {
        'Authorization': `Bearer ${token}`,
        'Content-Type': 'application/json'
    };
    const response = yield node_fetch_1.default(url, { method: 'POST', headers, body: JSON.stringify({}) });
    if (response.ok !== true) {
        throw new Error(`(errid 11) Something went wrong on 'POST' to ${url}: ${response.status} - ${response.statusText}`);
    }
    const deposition = yield response.json();
    if (verbose) {
        console.log(`creating a new, empty version in a new concept...done. id=${deposition.record_id}`);
    }
    return deposition.record_id.toString();
});
exports.deposition_create_concept = deposition_create_concept;
//# sourceMappingURL=concept.js.map